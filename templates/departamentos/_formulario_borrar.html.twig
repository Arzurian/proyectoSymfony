<form method="post" action="{{ path('borrar_departamento',{"cod_departamento": departamento.cod_departamento}) }}" onsubmit="return confirm('Seguro?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token("delete") }}">
    <button class="btn btn-danger">Eliminar departamento</button>
</form>
