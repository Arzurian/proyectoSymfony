{% extends "base.html.twig" %}
{%  block title %}Editar perfil{% endblock %}

{% block body %}
    <h2>Editar contraseña</h2>
    <div class="row justify-content-center">
        <div class="col-6">
            {{ form_start(form, {'method': 'POST'}) }}
            {{ form_widget(form.cod_usuario,{
                    "value": app.user.username,
                }) }}
            <div class="form-group row">
                <div class="col-6">
                    {{ form_label(form.pass_vieja,"Contraseña antigua") }}
                    {{ form_widget(form.pass_vieja,{
                        "attr": {"class":"form-control"}
                    }) }}
                </div>
            </div>

            <div class="form-group row">
                <div class="col-6">
                    {{ form_label(form.pass_nueva_uno,"Contraseña nueva") }}
                    {{ form_widget(form.pass_nueva_uno,{
                        "attr":{"class":"form-control",
                    }})}}
                </div>
            </div>
                
            <div class="form-group row">
                <div class="col-6">
                    {{ form_label(form.pass_nueva_dos,"Contraseña nueva") }}
                    {{ form_widget(form.pass_nueva_dos,{
                        "attr":{"class":"form-control",
                    }})}}
                </div>
            </div>

{#            <button class="btn btn-primary">{{ button_label|default("Modificar") }}</button>#}

            {{ form_end(form) }}
            <a class="btn btn-secondary btn-sm" href="{{ path("perfil_index") }}">Atras</a>
        </div>
{% endblock %}
